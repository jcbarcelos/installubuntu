autoinstall:
    version: 1
    identity:
        realname: 'Júlio César'
        hostname: ubuntu-desktop
        username: jc
        password: '$6$.7PR4Fjwsf4VPGhG$O5PjMjRrLs8bLR6OjS5.KLo6j/kt5/2Ac6Jv1hOUtbAz.JNIE/2yCdfhZJkK82ZG.7tvYlNBDbrUdFjZyCUEz/'
    locale: en_US.UTF-8
    keyboard:
        layout: br
    timezone: "America/Sao_Paulo"
    packages:
        - gimp
        - git
        - wget
        - adb
        - zsh
        - pipx
        - python3-full
        - snapd
        - curl
        - clang
        - ninja-build
        - ffmpeg
        - cmake
        - jq
        - gpg
        - curl
        - gh
        - ca-certificates
        - gnupg
        - dirmngr
        - gawk

    snaps:
        - name: gnome-font-viewer
          channel: stable
          classic: false
        - name: bolls
          channel: stable
          classic: false
        - name: bluemail
          channel: stable
          classic: false
        - name: telegram-desktop
          channel: stable
          classic: false
        - name: pencil
          channel: stable
          classic: false
        - name: audacity
          channel: stable
          classic: false
        - name: discord
          channel: stable
          classic: false
        - name: steam
          channel: stable
          classic: false
        - name: docker
          channel: stable
          classic: false
        - name: code
          channel: stable
          classic: true
        - name: gitkraken
          channel: stable
          classic: true
        - name: intellij-idea-community
          channel: stable
          classic: true
        - name: android-studio
          channel: stable
          classic: true
        - name: brave
          channel: stable
          classic: false
    codecs:
        install: true
    drivers:
        install: true
    updates: all
    shutdown: reboot
    storage:
        layout:
            name: manual

    # late-commands:
    #   - echo "Executando pós-instalação" >> /target/root/late-commands.log
    #   - set -x
    #   - curtin in-target -- sh -c "apt update && apt install -y gnome-software-plugin-flatpak"
    #   - curtin in-target -- sh -c "sh -c '$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)'"
    #   - curtin in-target -- sh -c "mkdir -p '${ASDF_DATA_DIR:-$HOME/.asdf}/completions'"
    #   - curtin in-target -- sh -c "asdf completion zsh > '${ASDF_DATA_DIR:-$HOME/.asdf}/completions/_asdf'"
    #   - curtin in-target -- sh -c "git clone https://github.com/asdf-vm/asdf.git --branch v0.16.0"
    #   - curtin in-target -- sh -c "export PATH='${ASDF_DATA_DIR:-$HOME/.asdf}/shims:$PATH'"
    #   - curtin in-target -- sh -c "chsh -s $(which zsh)"
    #   - curtin in-target -- sh -c "echo "alias update='sudo apt update && sudo apt upgrade -y && sudo apt autoremove -y && sudo apt-get clean && sudo apt-get autoclean && sudo killall snap-store; sudo snap refresh && flatpak update -y;' >> /home/$USER/.bashrc"
    #   - curtin in-target -- sh -c "echo "alias update='sudo apt update && sudo apt upgrade -y && sudo apt autoremove -y && sudo apt-get clean && sudo apt-get autoclean && sudo killall snap-store; sudo snap refresh && flatpak update -y;' >> /home/$USER/.zshrc"
    #   - curtin in-target -- sh -c "source ~/.zshrc"
    #   - curtin in-target -- sh -c "asdf plugin add nodejs https://github.com/asdf-vm/asdf-nodejs.git"

    #   - curtin in-target -- sh -c "wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb -O /tmp/chrome.deb"
    #   - curtin in-target -- sh -c "dpkg -i /tmp/chrome.deb || apt-get -f install -y"
    #   - curtin in-target -- sh -c "rm -rf /tmp/chrome.deb"

    #   - curtin in-target -- sh -c "asdf install nodejs latest"
    #   - curtin in-target -- sh -c "asdf plugin add"
    #   -	curtin in-target -- sh -c "asdf install java openjdk-11"
    #   - curtin in-target -- sh -c "asdf install java latest"
    #   - curtin in-target -- sh -c "asdf global java openjdk-11"
    #   - curtin in-target -- sh -c "asdf install flutter latest"
    #   - curtin in-target -- sh -c "asdf install flutter latest"
    #   - curtin in-target -- sh -c "asdf global flutter latest"
    #   - curtin in-target -- sh -c "asdf install pyhton latest"
    #   - curtin in-target -- sh -c "asdf install python 3.6.12"
    #   - curtin in-target -- sh -c "systemctl start docker.service"
    #   - curtin in-target -- sh -c "chown $USER:$USER /var/run/docker.sock"
    #   - curtin in-target -- sh -c "usermod -aG docker $USER"


    network:
        version: 2
        ethernets:
            eth0:
                dhcp4: true
        wifis:
            wlan0:
                dhcp4: true
                access-points:
                    "jt_g5":
                        password: "$6$LsC2zGgTzq39PTmV$h1f63kzAXuwGOI8sihhW3m05enuCH2/UDFOGRHpcBQaGGl4QaL8IBLJIqwvhbpp9/RLvwbW9wUGDbrcpKLfDg1"
